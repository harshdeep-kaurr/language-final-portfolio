# Portugal Sign Quest

Interactive React + Tailwind demo exploring linguistic landscapes in Portugal.

Quick start

1. Install dependencies

```bash
npm install
```

2. Run dev server

```bash
npm run dev
```

This demo uses Vite, Tailwind CSS, Framer Motion, and Lucide React icons. All state is local; no backend is required.
